#ifndef LENGHTHS_H
#define LENGHTHS_H


class Lenghths
{
    public:

       //function to convert meter to centimeters
       double met_to_cen(double met);

       //function to convert meter to inches
       double met_to_inc(double met);

       //function to convert meter to feet
       double met_to_fet(double met);

       //function to convert meter to miles
       double met_to_mil(double met);

       //function to convert meter to kilometers
       double met_to_kil(double met);

       //function to convert centimeter to meter
       double cen_to_met(double cen);

       //function to convert centimeter to inches
       double cen_to_inc(double cen);

       //function to convert centimeter to feet
       double cen_to_fet(double cen);

       //function to convert centimeter to miles
       double cen_to_mil(double cen);

       //function to convert centimeter to kilometer
       double cen_to_kil(double cen);


       //function to convert kilometer to meters
       double kil_to_met(double kil);

       //function to convert kilometer to centimeters
       double kil_to_cen(double kil);

       //function to convert kilometers to inches
       double kil_to_inc(double kil);

       //function to convert kilometers to miles
       double kil_to_mil(double kil);

       //function to convert kilometers to feet
       double kil_to_fet(double kil);

       //function to change miles to meters
       double mil_to_met(double mil);

       //function to convert miles to kilometers
       double mil_to_kil(double mil);

       //function to convert miles to centimeters
       double mil_to_cen(double cen);

       //function to convert miles to inches
       double mil_to_inc(double mil);

       //function to convert miles to feet
       double mil_to_fet(double mil);



};

#endif // LENGHTHS_H
